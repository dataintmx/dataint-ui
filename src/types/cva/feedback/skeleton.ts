/**
 * Skeleton has no variants.
 * It is always a rectangular block with rounded corners and a shimmer animation.
 * Width and height are controlled by consumers via utility classes or inline styles.
 */
export type SkeletonProps = {
    class?: string;
    className?: string;
};
