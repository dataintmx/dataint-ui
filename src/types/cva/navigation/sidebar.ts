/**
 * Sidebar tooltip: no variants — single base class + pseudo-element.
 * Included for discoverability and future extensibility.
 */
export type SidebarTooltipVariants = Record<string, never>;

/**
 * Sidebar nav icon: no variants — styling driven by parent hover.
 */
export type SidebarNavIconVariants = Record<string, never>;
