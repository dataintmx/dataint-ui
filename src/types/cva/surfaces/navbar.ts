/**
 * Variants for the Navbar component.
 *
 * For now, Navbar is intentionally opinionated:
 * - Styling is fully defined by the `.navbar` utility.
 * - No consumer-facing variants are required.
 *
 * This type exists to:
 * - Keep consistency with the CVA architecture
 * - Allow future extension without breaking changes
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export type NavbarVariants = {};
